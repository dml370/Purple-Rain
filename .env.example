# AI Companion - Environment Configuration
# Rename this file to .env and fill in all values for your deployment.

# --- Core Application Settings ---
FLASK_ENV=production
# CRITICAL: Generate a long, random, secret string for session security.
FLASK_SECRET_KEY=generate-a-very-strong-random-string-here
# The public domain name of your server, e.g., doitinglobal.info
SERVER_NAME=
# For production security, set this to your exact frontend URL, e.g., https://doitinglobal.info
CORS_ORIGIN=

# --- Licensing & Encryption ---
# CRITICAL: A 32-byte URL-safe base64 key for the primary server to GENERATE licenses.
LICENSE_MASTER_KEY=
# CRITICAL: A separate 32-byte key for encrypting sensitive fields (like API keys) in the database.
FIELD_ENCRYPTION_KEY=
# For a deployed Personal Agent instance, this holds its own unique license key.
LICENSE_KEY=

# --- Database Connection ---
# For Local Development using Docker Compose (Postgres):
# DATABASE_URI=postgresql://admin:password@db:5432/ai_companion_db
# DB_USER=admin
# DB_PASSWORD=password
# DB_NAME=ai_companion_db

# For Production (Oracle Autonomous Database):
DATABASE_URI=oracle+oracledb://USER:PASSWORD@TNS_NAME_FROM_WALLET
ORACLE_WALLET_PATH=/path/to/your/unzipped/adb/wallet
ORACLE_WALLET_PASSWORD=your-wallet-password

# --- Google OAuth for User Login ---
# Get these from your Google Cloud Platform API & Services console.
GOOGLE_LOGIN_CLIENT_ID=
GOOGLE_LOGIN_CLIENT_SECRET=

# --- Bootstrap Agent (Runs on your primary server) ---
# The API key for the initial guidance agent.
BOOTSTRAP_AGENT_PROVIDER=openai
BOOTSTRAP_AGENT_API_KEY=
