version: '3'
services:
  ai-companion:
    build: .
    ports:
      - "80:5000"
    environment:
      - PORT=5000
      - OAUTH_REDIRECT_URI=https://yourdomain.com/oauth/callback
    volumes:
      - ./data:/mnt/companion_memory